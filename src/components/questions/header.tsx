import Link from "next/link";

const Header = () => {
  return (
    <header>
      <nav className="p-2 text-center">
        <Link href="/">yametara</Link>
      </nav>
    </header>
  );
};

export default Header;
